'use client';
import React from 'react'

const AddToCart = () => {
  return (
    <div>
        <button onClick={() => console.log('clicked')}>Click me</button>
    </div>
  )
}

export default AddToCart